% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core-plot-object.R
\name{+.reaborn_plot}
\alias{+.reaborn_plot}
\title{Add to a reaborn plot, preserving its class}
\usage{
\method{+}{reaborn_plot}(e1, e2)
}
\arguments{
\item{e1}{A reaborn plot.}

\item{e2}{A ggplot component (geom, scale, theme, facet, ...).}
}
\value{
A reaborn plot.
}
\description{
Ensures \verb{reaborn_plot + <ggplot component>} stays a \code{reaborn_plot} so the
composed object keeps printing through reaborn and remains chainable.
}
\details{
Categorical plots collapse their data (via \code{rb_cat_setup()}, and for
bar/point/count an estimator aggregation), so the facet variable is absent
from the plotted data. When such a plot carries a re-aggregation hook and the
user adds a facet, we rebuild it with the facet columns forwarded through
setup + aggregation (the same \code{.facet_vars} path \code{catplot()} uses) and then
apply the user's facet -- so each panel shows, and for aggregating kinds
re-estimates, its own subset instead of one shared full-data summary (#73).
}
\keyword{internal}
