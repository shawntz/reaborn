% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palettes.R
\name{color_palette}
\alias{color_palette}
\alias{hls_palette}
\alias{husl_palette}
\alias{dark_palette}
\alias{light_palette}
\alias{diverging_palette}
\alias{blend_palette}
\alias{mpl_palette}
\alias{cubehelix_palette}
\title{Return a list of colors or a continuous colormap defining a palette}
\usage{
color_palette(palette = NULL, n_colors = NULL, desat = NULL, as_cmap = FALSE)

hls_palette(n_colors = 6, h = 0.01, l = 0.6, s = 0.65, as_cmap = FALSE)

husl_palette(n_colors = 6, h = 0.01, s = 0.9, l = 0.65, as_cmap = FALSE)

dark_palette(
  color,
  n_colors = 6,
  reverse = FALSE,
  as_cmap = FALSE,
  input = "rgb"
)

light_palette(
  color,
  n_colors = 6,
  reverse = FALSE,
  as_cmap = FALSE,
  input = "rgb"
)

diverging_palette(
  h_neg,
  h_pos,
  s = 75,
  l = 50,
  sep = 1,
  n = 6,
  center = "light",
  as_cmap = FALSE
)

blend_palette(colors, n_colors = 6, as_cmap = FALSE, input = "rgb")

mpl_palette(name, n_colors = 6, as_cmap = FALSE)

cubehelix_palette(
  n_colors = 6,
  start = 0,
  rot = 0.4,
  gamma = 1,
  hue = 0.8,
  light = 0.85,
  dark = 0.15,
  reverse = FALSE,
  as_cmap = FALSE
)
}
\arguments{
\item{palette}{\code{NULL}, a string, or a sequence of colors.}

\item{n_colors}{Number of colors. If \code{NULL}, depends on \code{palette}.}

\item{desat}{Proportion to desaturate each color by.}

\item{as_cmap}{If \code{TRUE}, return a continuous colormap (a \code{reaborn_cmap}).}

\item{h, l, s}{Hue, lightness, saturation anchors in \verb{[0, 1]}.}

\item{color}{Base color for the high end of a sequential palette.}

\item{reverse}{Reverse the direction of the blend.}

\item{input}{Color space of the input color: \code{"rgb"}, \code{"hls"}, or \code{"husl"}.}

\item{h_neg, h_pos}{Anchor hues (\verb{[0, 359]}) for the negative and positive ends.}

\item{sep}{Size of the intermediate (center) region.}

\item{n}{Number of colors (when not returning a cmap).}

\item{center}{\code{"light"} or \code{"dark"} center.}

\item{colors}{A sequence of colors to blend between.}

\item{name}{Name of a matplotlib colormap.}

\item{start, rot, gamma, hue, light, dark}{Cubehelix parameters (see seaborn).}
}
\value{
A character vector of hex colors, or a \code{reaborn_cmap}.
}
\description{
Port of \code{seaborn.color_palette}. Possible \code{palette} values include the name
of a seaborn palette (\code{deep}, \code{muted}, \code{bright}, \code{pastel}, \code{dark},
\code{colorblind}), a matplotlib colormap name, \code{"husl"}/\code{"hls"}, a cubehelix
shorthand (\code{"ch:..."}), \code{"light:<color>"}, \code{"dark:<color>"},
\code{"blend:<c1>,<c2>"}, or a sequence of colors.
}
\examples{
palplot(color_palette("deep"))
palplot(color_palette("husl", 8))
palplot(hls_palette(8))
palplot(cubehelix_palette(8))
palplot(light_palette("seagreen"))
palplot(diverging_palette(220, 20))
}
